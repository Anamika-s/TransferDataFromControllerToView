@model WebApplication7.Models.Numbers

@{
    ViewBag.Title = "Add4";
}

<h2>Add3</h2>

@{ if (ViewBag.res != null)
    {
        <b> Result is @ViewBag.res </b>
    }
    else
    {
        using (Html.BeginForm())
        {
            <div>
                @Html.Label("Enter No1")
                @Html.TextBoxFor(x=>x.No1)
            </div>
            <div>
                @Html.Label("Enter No2")
                @Html.TextBoxFor(x => x.No2)
            </div>

            <input type="submit" value="Add" />

        }
    }

}



